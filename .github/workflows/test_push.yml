name: Test ntfy Push

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send test push
        run: |
          curl -d "🚀 GitHub Test Push funktioniert!" ${{ secrets.NTFY_URL }}
